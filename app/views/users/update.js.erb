// function refreshForm(innerHTML) {
//   const newBioForm = document.getElementById('formTarget');
//     newBioForm.innerHTML = innerHTML;
// };
function addBio() {
  const bio = document.getElementById('bio-text');
  const newBio = document.getElementById('user_bio').value
  bio.innerText = newBio;
};

addBio();
// addBio("<%= j render 'users/bio', user: @user %>");
// refreshForm("<%= j render 'users/form', user: @user  %>");

function addAbility() {
  const abilityText = document.getElementById("user-ability-text");
  const newAbilityText = document.getElementById('user_ability').value;
  abilityText.innerHTML = newAbilityText;
}

addAbility();
